// File: lib/utils/constants.dart
// TODO: Centralize Static Assets and Magic Numbers
// Requirements:
// 1. `class AppColors`: Define static references if needed outside Theme.
// 2. `class AppConstants`: Max Title lengths, base padding values, Animation Duration constants (e.g. `const appInterceptionDuration = Duration(milliseconds: 500)`(NFR-01)).
